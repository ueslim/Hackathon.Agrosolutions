apiVersion: apps/v1
kind: Deployment
metadata:
  name: farm-api
  namespace: default
  labels:
    app: farm-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: farm-api
  template:
    metadata:
      labels:
        app: farm-api
    spec:
      containers:
      - name: farm-api
        image: your-dockerhub-username/agrosolutions-farm-api:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: ASPNETCORE_URLS
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: ASPNETCORE_URLS
        - name: ASPNETCORE_ENVIRONMENT
          valueFrom:
            configMapKeyRef:
              name: agrosolutions-config
              key: ASPNETCORE_ENVIRONMENT
        - name: ConnectionStrings__FarmDb
          valueFrom:
            secretKeyRef:
              name: agrosolutions-secrets
              key: FARM_DB_CONNECTION
        - name: JWT__Secret
          valueFrom:
            secretKeyRef:
              name: agrosolutions-secrets
              key: JWT_SECRET
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 40
          periodSeconds: 30
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: farm-api
  namespace: default
  labels:
    app: farm-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: farm-api

